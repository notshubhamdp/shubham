// admin_dashboard.js
// Add interactivity for admin dashboard here

document.addEventListener('DOMContentLoaded', function() {
    // Example: Show welcome message
    const welcome = document.getElementById('welcomeMsg');
    if (welcome) {
        welcome.style.color = '#38bdf8';
    }
    // Add more admin-specific JS logic here
});
